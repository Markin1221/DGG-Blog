// Seleciona o botão pelo ID
const button = document.getElementById('sobre nos');

// Adiciona um evento de clique ao botão
button.addEventListener('click', function() {
    console.log('Botão clicado!'); // Exibe uma mensagem no console
    alert('click'); // Exibe um alerta
});